import 'package:flutter/material.dart';

class GroupInfo extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('GroupInfo'),
      ),
    );
  }
}
